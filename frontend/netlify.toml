[build]
  base    = "frontend"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback для react-router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
